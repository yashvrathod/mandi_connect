// Expo Router app entrypoint.
// This project uses `"main": "expo-router/entry"` in package.json.
// Keep this file as a simple re-export so TypeScript can compile cleanly.

export { default } from "expo-router/entry";
